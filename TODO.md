# TODO

- [ ] Fix `getFaviconUrl is not defined` by implementing `getFaviconUrl()` in `app.js`.
- [ ] Ensure favicon URLs are safely generated (support http/https, handle missing scheme).
- [ ] Run a quick sanity check (search for remaining references / ensure code compiles).

